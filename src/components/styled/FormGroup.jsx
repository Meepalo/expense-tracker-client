import styled from "@emotion/styled";

const FormGroup = styled.div`
	margin: 20px 0;

	label {
		display: block;
		font-weight: 800;
		font-size: 0.9rem;
		margin-bottom: 4px;
	}
`;

export default FormGroup;
